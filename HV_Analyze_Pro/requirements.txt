# HVSR Pro - Complete Dependencies
# Install with: pip install -r requirements.txt
# Python 3.10+ recommended

# ── Core scientific stack ──────────────────────────────────────
numpy>=1.24,<3
scipy>=1.10,<2
matplotlib>=3.7,<4
pandas>=2.0,<3
openpyxl>=3.1              # Excel read/write (pandas optional dep)

# ── Seismology ─────────────────────────────────────────────────
obspy>=1.4                 # MiniSEED / SAC / seismic data I/O

# ── GUI framework ──────────────────────────────────────────────
PyQt5>=5.15
PyQtWebEngine>=5.15        # QWebEngineView for interactive maps

# ── Interactive maps & visualization ───────────────────────────
folium>=0.17               # Generates Leaflet HTML maps
branca>=0.7                # Folium colormap backend
plotly>=5.18               # Interactive 3D plots

# ── Geospatial / coordinate transforms ─────────────────────────
pyproj>=3.6                # CRS conversions (UTM ↔ WGS84)

# ── Machine learning ──────────────────────────────────────────
scikit-learn>=1.4          # Window clustering / classification

# ── 3D Bedrock Mapping ────────────────────────────────────────
pykrige>=1.7               # Kriging interpolation methods
shapely>=2.0               # Boundary geometry operations
pyvista>=0.43              # 3D mesh visualization & export
Pillow>=10.0               # Contour image generation

# ── HV Strip Progressive ──────────────────────────────────────
PyYAML>=6.0                # Settings persistence
seaborn>=0.12              # Enhanced statistical plots

# ── HVSR Inversion ────────────────────────────────────────────
pyswarms>=1.3              # Particle swarm optimization
tqdm>=4.60                 # Progress bars

# ── Templating (KMZ export) ───────────────────────────────────
jinja2>=3.1                # HTML template rendering

# ── Build / packaging ─────────────────────────────────────────
setuptools>=68

# ── Testing (development only) ────────────────────────────────
pytest>=8.0
